import initButtonControl from './buttonControl';
import initKeyboardControl from './keyboardControl';

const initControls = () => {
  initButtonControl();
  initKeyboardControl();
};

export default initControls;
